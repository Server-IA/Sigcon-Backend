package com.sigcon.backend.assets.disposals.domain.model.enums;

/**
 * Tipo de disposicion de activo.
 * BAJA: activo dado de baja (venta, destruccion, obsolescencia).
 * TRANSFERENCIA: activo transferido a otra area o entidad.
 */
public enum DisposalType {
    BAJA,
    TRANSFERENCIA
}
