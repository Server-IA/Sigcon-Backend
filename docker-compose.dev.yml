version: "3.8"

# Docker Compose para desarrollo con Swagger habilitado
# Uso: docker-compose -f docker-compose.dev.yml up --build

services:
  backend:
    build: .
    container_name: backend-dev-api
    ports:
      - "${backend_port_api:-8080}:${backend_port_api:-8080}"
    depends_on:
      - ${backend_host:-db}
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://${backend_host:-db}:${backend_port_db_dev:-5443}/${backend_database:-backend}
      SPRING_DATASOURCE_USERNAME: ${backend_user:-postgres}
      SPRING_DATASOURCE_PASSWORD: ${backend_password:-postgres}
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
      # SPRING_PROFILES_ACTIVE: dev  # Habilita Swagger
      LOGGING_LEVEL_ORG_SPRINGFRAMEWORK_WEB: DEBUG
      LOGGING_LEVEL_COM_SIGCON_BACKEND: DEBUG
      SPRING_PROFILES_ACTIVE: dev  

    deploy:
      resources:
        limits:
          memory: 512M  # Más memoria para desarrollo

  db:
    image: postgres:15
    container_name: backend-dev-db
    restart: always
    environment:
      POSTGRES_DB: ${backend_database:-backend}
      POSTGRES_USER: ${backend_user:-postgres}
      POSTGRES_PASSWORD: ${backend_password:-postgres}
    ports:
      - "${backend_port_db_dev:-5443}:${backend_port_db_dev:-5443}"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    volumes:
      - postgres_data_dev:/var/lib/postgresql/data

  # adminer:
  #   image: adminer
  #   container_name: backend-dev-adminer
  #   restart: always
  #   ports:
  #     - "${backend_port_adminer:-8081}:${backend_port_api:-8080}"
  #   depends_on:
  #     - ${backend_host:-db}

volumes:
  postgres_data_dev: